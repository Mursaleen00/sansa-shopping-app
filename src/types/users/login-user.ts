// src/types/users/login-user.ts

// Export interface LoginType
export interface LoginType {
  accessToken: string;
  refreshToken: string;
  id: number;
  username: string;
  email: string;
  firstName: string;
  lastName: string;
  gender: string;
  image: string;
}
